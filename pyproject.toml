[project]
name = "chatbot-base"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Vigil Varghese", email = "vigilvworks@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openai>=3.1.0",
    "python-dotenv>=1.2.3",
    "tiktoken>=0.14.0",
]

[project.scripts]
chatbot-base = "chatbot_base.main:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
